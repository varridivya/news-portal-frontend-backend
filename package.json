{
  "name": "news-api-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:server": "cd backend && npm install && npm run start:server",
    "start:client": "cd frontend && npm install && npm run start:client"
  }
}